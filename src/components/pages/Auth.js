import dayjs from 'dayjs';
import React from 'react';
import styled from 'styled-components';

function Auth() {
  const date = new Date();
  const day = new dayjs();
  return <div></div>;
}

export default Auth;
